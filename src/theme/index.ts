import { Theme } from '@emotion/react'

import { COLOR_THEME } from './color'
import { MEDIA_THEME } from './media'
import { TYPOGRAPHY_THEME } from './typography'

export const theme: Theme = {
  color: COLOR_THEME,
  typography: TYPOGRAPHY_THEME,
  media: MEDIA_THEME,
}
